// Importaciones basicas
import React from "react";
import ReactDOM from "react-dom/client";

// Importaciones
import { App, Productos } from "./App";

// Creador
const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <>
    <App />
  </>
);
